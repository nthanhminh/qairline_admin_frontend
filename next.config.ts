import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['assets.aboutamazon.com', 'amazon-blogs-brightspot.s3.amazonaws.com'],
  }
};

export default nextConfig;
